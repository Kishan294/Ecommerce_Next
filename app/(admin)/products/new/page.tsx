import ProductForm from "@/components/ProductForm";
import React from "react";

const page = () => {
  return (
    <div className="px-2">
      <ProductForm heading="Add New Product" />
    </div>
  );
};

export default page;
